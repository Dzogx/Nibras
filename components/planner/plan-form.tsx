"use client";
import { useActionState } from "react";
import { createAnnualPlanAction } from "@/app/(app)/planner/plan-actions";
export function PlanForm({ classes }: { classes: { id: string; name: string; label: string }[] }) { const [state, action, pending] = useActionState(createAnnualPlanAction, {}); if (!classes.length) return <p className="rounded-xl border border-dashed border-slate-300 p-5 text-sm text-slate-600">أضف قسماً أولاً قبل إنشاء الخطة السنوية.</p>; return <form action={action} className="mt-5 grid gap-3 rounded-xl border border-slate-200 bg-white p-5 shadow-sm sm:grid-cols-2"><label className="text-sm">القسم<select name="classId" required className="mt-1 w-full rounded-md border border-slate-300 p-2">{classes.map((item) => <option key={item.id} value={item.id}>{item.name} — {item.label}</option>)}</select></label><label className="text-sm">عنوان الخطة<input name="title" required defaultValue="الخطة السنوية للاجتماعيات" className="mt-1 w-full rounded-md border border-slate-300 p-2" /></label><div className="sm:col-span-2"><button disabled={pending} className="rounded-lg bg-emerald-700 px-4 py-2 text-sm font-semibold text-white disabled:opacity-60">{pending ? "جارٍ الإنشاء…" : "إنشاء الخطة"}</button>{state.error && <p className="mt-2 text-sm text-red-700">{state.error}</p>}</div></form>; }
