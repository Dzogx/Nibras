"use client";
import { useActionState } from "react";
import { linkContentToPlanItem, type LinkActionState } from "@/app/(app)/studio/[contentId]/link-action";
type Item={id:string;title:string};
export function ContentPlanLink({contentId,items,currentPlanItemId}:{contentId:string;items:Item[];currentPlanItemId:string|null}){const action=linkContentToPlanItem.bind(null,contentId);const initial:LinkActionState={};const[state,formAction,pending]=useActionState(action,initial);return <section className="mt-5 rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold">ربط المورد بالخطة</h2><p className="mt-1 text-sm text-slate-600">{currentPlanItemId?"هذا المورد مرتبط حالياً ببند من الخطة ويمكن تغيير الربط.":"اربط المورد ببند من Teacher OS ليصبح جزءاً من تسلسل التنفيذ."}</p>{items.length?<form action={formAction} className="mt-3 flex flex-wrap gap-2"><select name="planItemId" defaultValue={currentPlanItemId??""} className="min-w-64 flex-1 rounded-md border border-slate-300 p-2 text-sm"><option value="" disabled>اختر بند الخطة</option>{items.map(item=><option key={item.id} value={item.id}>{item.title}</option>)}</select><button disabled={pending} className="rounded-md bg-emerald-700 px-3 py-2 text-sm font-semibold text-white">{pending?"جارٍ الحفظ…":"ربط المورد"}</button></form>:<p className="mt-3 text-sm text-amber-800">لا توجد بنود خطة مسجلة بعد.</p>}{state.error&&<p className="mt-2 text-sm text-red-700">{state.error}</p>}</section>}
