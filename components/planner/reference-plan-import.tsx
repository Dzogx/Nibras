"use client";
import { useState } from "react";
import { importReferencePlanAction } from "@/app/(app)/plans/[planId]/actions";
export function ReferencePlanImport({ planId, hasItems }: { planId: string; hasItems: boolean }) { const [state, setState] = useState<{ error?: string; imported?: number }>({}); const [pending, setPending] = useState(false); if (hasItems) return null; async function importPlan() { setPending(true); setState(await importReferencePlanAction(planId)); setPending(false); } return <div className="mt-5 rounded-xl border border-emerald-200 bg-emerald-50 p-4"><b className="text-emerald-950">استيراد المقاطع المرجعية</b><p className="mt-1 text-sm text-emerald-900">متاح حالياً لمسار تاريخ الأولى متوسط الموثق. ستضاف المقاطع مع الكفاءة والمصدر والصفحة.</p><button type="button" onClick={importPlan} disabled={pending} className="mt-3 rounded-lg bg-emerald-700 px-4 py-2 text-sm font-semibold text-white disabled:opacity-60">{pending ? "جارٍ الاستيراد…" : "استيراد المقاطع المرجعية"}</button>{state.error && <p className="mt-2 text-sm text-red-700">{state.error}</p>}{state.imported && <p className="mt-2 text-sm text-emerald-800">تم استيراد {state.imported} مقاطع. حدّث الصفحة لعرضها.</p>}</div>; }
