"use client";
import { useMemo, useState } from "react";
import { getAssessmentStructure } from "@/lib/assessment/structure-policy";
import type { AssessmentGrade, AssessmentSubject } from "@/lib/assessment/scoring-policy";
const subjects: { value: AssessmentSubject; label: string }[] = [{ value: "history", label: "التاريخ" }, { value: "geography", label: "الجغرافيا" }, { value: "civic-education", label: "التربية المدنية" }];
const grades: { value: AssessmentGrade; label: string }[] = [{ value: "1am", label: "الأولى متوسط" }, { value: "2am", label: "الثانية متوسط" }, { value: "3am", label: "الثالثة متوسط" }, { value: "4am", label: "الرابعة متوسط" }];
export function AssessmentStructureViewer() { const [grade, setGrade] = useState<AssessmentGrade>("4am"); const [subject, setSubject] = useState<AssessmentSubject>("history"); const structure = useMemo(() => getAssessmentStructure(grade, subject), [grade, subject]); return <div><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm">المستوى<select value={grade} onChange={(event) => setGrade(event.target.value as AssessmentGrade)} className="mt-1 w-full rounded-lg border border-slate-300 bg-white p-3">{grades.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select></label><label className="text-sm">المادة<select value={subject} onChange={(event) => setSubject(event.target.value as AssessmentSubject)} className="mt-1 w-full rounded-lg border border-slate-300 bg-white p-3">{subjects.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select></label></div><article className="mt-5 rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="font-bold">بنية التقويم التحصيلي</h2><span className="rounded-full bg-emerald-50 px-3 py-1 text-sm font-semibold text-emerald-800">{structure.totalPoints} نقطة</span></div>{structure.official ? <div className="mt-4 space-y-3">{structure.parts.map((part) => <div key={part.label} className="rounded-lg bg-slate-50 p-4"><div className="flex justify-between gap-3"><b>{part.label}</b><b className="text-emerald-700">{part.points} نقاط</b></div><p className="mt-1 text-sm text-slate-600">{part.description}</p><p className="mt-2 text-xs text-slate-500">مرجع الدليل: ص {part.sourcePage}</p></div>)}</div> : <p className="mt-4 rounded-lg bg-amber-50 p-4 text-sm text-amber-900">{structure.guidance}</p>}<p className="mt-4 text-xs text-slate-500">{structure.guidance}</p></article></div>; }
