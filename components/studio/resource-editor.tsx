"use client";
import { useActionState } from "react";
import { updateLessonResource,type EditContentState } from "@/app/(app)/studio/[contentId]/edit-action";
type Body={targetedCompetence:string;evidenceOfLearning:string;activity:{instruction:string;durationMinutes:number;expectedProduct:string};formativeAssessment:string};
export function ResourceEditor({contentId,title,body}:{contentId:string;title:string;body:Body}){const action=updateLessonResource.bind(null,contentId);const initial:EditContentState={};const[state,formAction,pending]=useActionState(action,initial);return <form action={formAction} className="space-y-4 rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><label className="block text-sm">العنوان<input name="title" defaultValue={title} className="mt-1 w-full rounded border border-slate-300 p-2"/></label><label className="block text-sm">الكفاءة المستهدفة<textarea name="competence" defaultValue={body.targetedCompetence} rows={3} className="mt-1 w-full rounded border border-slate-300 p-2"/></label><label className="block text-sm">دليل الإتقان<textarea name="evidence" defaultValue={body.evidenceOfLearning} rows={2} className="mt-1 w-full rounded border border-slate-300 p-2"/></label><label className="block text-sm">تعليمة النشاط<textarea name="activity" defaultValue={body.activity.instruction} rows={4} className="mt-1 w-full rounded border border-slate-300 p-2"/></label><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm">الزمن<input name="minutes" type="number" defaultValue={body.activity.durationMinutes} className="mt-1 w-full rounded border border-slate-300 p-2"/></label><label className="text-sm">منتج المتعلم<input name="product" defaultValue={body.activity.expectedProduct} className="mt-1 w-full rounded border border-slate-300 p-2"/></label></div><label className="block text-sm">التقويم التكويني<textarea name="assessment" defaultValue={body.formativeAssessment} rows={2} className="mt-1 w-full rounded border border-slate-300 p-2"/></label><button disabled={pending} className="rounded-lg bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">{pending?"جارٍ الحفظ…":"حفظ إصدار جديد"}</button>{state.error&&<p className="text-sm text-red-700">{state.error}</p>}{state.ok&&<p className="text-sm text-emerald-700">تم حفظ إصدار جديد من المورد.</p>}</form>}
