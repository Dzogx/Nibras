import { createClient } from '@/lib/supabase/server';import {buildLessonMemo}from'@/lib/lesson/active-learning';
export default async function LessonPage({searchParams}:{searchParams:Promise<{situation?:string}>}){const{ situation }=await searchParams;const s=await createClient();const q=s.from('learning_situations').select('id,title,kind,competence,resources,suggested_minutes,subjects(name_ar),grade_levels(name_ar)');const{data:item}=situation?await q.eq('id',situation).single():await q.eq('kind','learning').order('sort_order').limit(1).single();if(!item)return <section><h1 className="text-3xl font-bold">حضّر حصة</h1><p className="mt-4 text-slate-600">لا توجد وضعية تعليمية متاحة بعد.</p></section>;const memo=buildLessonMemo({title:item.title,competence:item.competence??'تحدد الكفاءة من المرجع قبل الاعتماد.',resource:item.title,minutes:item.suggested_minutes??55,learners:40});return <section><p className="text-sm font-semibold text-emerald-700">Lesson Production Engine</p><h1 className="mt-1 text-3xl font-bold">مذكرة الحصة</h1><article className="mt-7 max-w-4xl rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-sm text-slate-600">الوضعية التعليمية: {item.title}</p><h2 className="mt-2 text-xl font-bold">{memo.title}</h2><h3 className="mt-5 font-bold">الكفاءة المستهدفة</h3><p>{memo.competence}</p><h3 className="mt-5 font-bold">أهداف التعلم</h3><ul className="list-disc pr-5">{memo.objectives.map(x=><li key={x}>{x}</li>)}</ul><h3 className="mt-5 font-bold">استراتيجية التعلم النشط</h3><p><b>{memo.strategy.name}</b> — {memo.strategy.why}</p><p className="mt-2 text-sm">تنظيم القسم: {memo.strategy.groupSize}<br/>المنتج: {memo.strategy.product}<br/>البديل: {memo.strategy.alternative}</p><h3 className="mt-5 font-bold">سير الحصة</h3>{memo.flow.map(x=><div key={x.phase} className="mt-2 rounded bg-slate-50 p-3"><b>{x.phase} — {x.minutes} دقيقة</b><p>{x.action}</p></div>)}<h3 className="mt-5 font-bold">التقويم التكويني</h3><p>فحص منتج المجموعات وبطاقة خروج قصيرة قبل نهاية الحصة.</p><h3 className="mt-5 font-bold">الواجب المنزلي</h3><p>إعادة توظيف مورد واحد من الحصة في مثال من محيط المتعلم.</p></article></section>}
