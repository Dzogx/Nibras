import "server-only";
export async function generateJson<T>(instructions:string,input:string):Promise<T>{const key=process.env.OPENAI_API_KEY;if(!key)throw new Error("OPENAI_API_KEY is not configured.");const response=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({model:"gpt-4.1-mini",instructions,input,text:{format:{type:"json_object"}},max_output_tokens:2200})});if(!response.ok)throw new Error(`OpenAI request failed: ${response.status}`);const data=await response.json() as {output_text?:string};if(!data.output_text)throw new Error("OpenAI returned no text.");return JSON.parse(data.output_text) as T;}
