import{describe,it,expect}from'vitest';import{buildLessonMemo,recommendStrategy}from'@/lib/lesson/active-learning';describe('lesson engine',()=>{it('recommends card sorting for a large class',()=>expect(recommendStrategy('الوثائق التاريخية',40).id).toBe('card-sort'));it('builds a timed memo',()=>expect(buildLessonMemo({title:'الوثائق',competence:'كفاءة',resource:'الوثائق',minutes:55,learners:40}).flow.reduce((s,x)=>s+x.minutes,0)).toBe(55));});
