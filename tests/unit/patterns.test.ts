import{describe,it,expect}from'vitest';import{recommendPattern}from'@/lib/assessment/patterns';describe('assessment patterns',()=>{it('keeps BEM separate from term coverage',()=>expect(recommendPattern('4am','social-studies','bem_practice').requiresCompletedCoverage).toBe(false));it('requires completed teaching coverage for term assessments',()=>expect(recommendPattern('2am','social-studies','summative_term').requiresCompletedCoverage).toBe(true));});
